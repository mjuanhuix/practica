
{% extends 'base.html.twig' %}

{% block body_id 'admin' %}

{% block header %}<header>

    <nav class="navbar navbar-default">
        <div class="container-fluid">

            <div class="navbar-header">
                <button type="button" class="navbar-toggle collapsed" data-toggle="collapse" data-target="#bs-example-navbar-collapse-1" aria-expanded="false">
                    <span class="sr-only">Toggle navigation</span>
                    <span class="icon-bar"></span>
                    <span class="icon-bar"></span>
                    <span class="icon-bar"></span>
                </button>
                <a class="navbar-brand" href="{{  path("homepage") }}">Practica final</a>
            </div>


            <div class="collapse navbar-collapse" id="bs-example-navbar-collapse-1">
                {% set route = app.request.get('_route')%}
                <ul class="nav navbar-nav">
                    {% block menu %}
                        <li class="{%  if route=="app_contacto" %} {{ "active" }}{% endif %}"><a href="">apartat 1<span class="sr-only"></span></a></li>
                        <li class="{%  if route=="app_productos" %} {{ "active" }}{% endif %}"><a href="">Apartat 2<span class="sr-only"></span></a></li>
                        <li class="{%  if route=="app_compania" %} {{ "active" }}{% endif %}"><a href="">apartat3<span class="sr-only"></span></a></li>
                        <li class="{%  if route=="app_login" %} {{ "active" }}{% endif %}"><a href="">Apartat 4<span class="sr-only"></span></a></li>
                    {% endblock menu %}
                </ul>

                <ul class="nav navbar-nav navbar-right">

                    <li><a> Bienvenido, {{ app.user.nombre }} </a></li>
                     <li><a href="{{ path('logout') }}">Logout</a></li>
                </ul>

            </div>
        </div>
    </nav>

</header>{% endblock %}


 {% block body %}

     <h1>{{ 'panel_jefeproyecto'|trans }}</h1>

{% endblock %}



